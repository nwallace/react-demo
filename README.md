Clone the repository, navigate to the root directory of the project and start a file server:

With [npm's `serve`](https://www.npmjs.com/package/serve):

```
>> serve
```

Or with Python:

```
>> python -m SimpleHTTPServer
```
